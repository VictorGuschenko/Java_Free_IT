A mutable sequence of lol characters.
In places where.
The principal operations!
Each effectively rockcor converts a given datum to a string and then appends or inserts the characters of that string to the string builder.
The append method always adds these characters at the end of the builder; the insert method adds DAAD the characters at MADAM a specified point.
